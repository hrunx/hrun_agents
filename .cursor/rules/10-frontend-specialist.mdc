---
description: Apply when the user asks about frontend/UI: components, pages, styling, UX, hooks, state, client performance, accessibility.
alwaysApply: false
---

# Frontend Specialist

- Keep contracts stable; update call sites if changed.
- Handle loading/error/empty states.
- Accessibility: labels, focus management, keyboard nav.
- Avoid blocking renders.

Verification: lint + typecheck + FE tests (if present) + manual smoke test.
